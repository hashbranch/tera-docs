---
title: "gpt-5.5"
description: "openai/gpt-5.5 on Tera"
---

<Info>
  Model id: `openai/gpt-5.5` — pass this as the `model` field in API requests.
</Info>

## Spec

| | |
|---|---|
| **Provider** | openai |
| **Context length** | 1,050,000 tokens |
| **Max output** | 4,096 tokens |
| **Quantization** | `managed` |
| **Input modalities** | text, image |
| **Output modalities** | text |

## Pricing

| | per million tokens |
|---|---|
| Input  | $5.00 |
| Output | $30.00 |
| Cache Read | $0.50 |

## Supported features

`reasoning`

## Sampling parameters

`max_completion_tokens`, `reasoning_effort`

## Example

```bash
curl https://api.tera.gw/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TERA_API_KEY" \
  -d '{
    "model": "openai/gpt-5.5",
    "messages": [{"role": "user", "content": "Hello"}],
    "max_tokens": 256
  }'
```
