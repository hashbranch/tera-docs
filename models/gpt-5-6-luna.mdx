---
title: "gpt-5.6-luna"
description: "openai/gpt-5.6-luna on Tera"
---

<Info>
  Model id: `openai/gpt-5.6-luna` — pass this as the `model` field in API requests.
</Info>

## Spec

| | |
|---|---|
| **Provider** | openai |
| **Context length** | 1,050,000 tokens |
| **Max output** | 4,096 tokens |
| **Quantization** | `managed` |
| **Input modalities** | text, image |
| **Output modalities** | text |

## Pricing

| | per million tokens |
|---|---|
| Input  | $1.00 |
| Output | $6.00 |
| Cache Read | $0.10 |

## Supported features

—

## Sampling parameters

`top_p`, `max_completion_tokens`, `stop`

## Example

```bash
curl https://api.tera.gw/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TERA_API_KEY" \
  -d '{
    "model": "openai/gpt-5.6-luna",
    "messages": [{"role": "user", "content": "Hello"}],
    "max_completion_tokens": 256
  }'
```
