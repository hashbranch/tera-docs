---
title: "gpt-oss-120b"
description: "openai/gpt-oss-120b on Tera"
---

<Info>
  Model id: `openai/gpt-oss-120b` — pass this as the `model` field in API requests.
</Info>

## Spec

| | |
|---|---|
| **Provider** | openai |
| **HuggingFace** | [`openai/gpt-oss-120b`](https://huggingface.co/openai/gpt-oss-120b) |
| **Context length** | 131,072 tokens |
| **Max output** | 4,096 tokens |
| **Quantization** | `mxfp4` |
| **Input modalities** | text |
| **Output modalities** | text |

## Pricing

| | per million tokens |
|---|---|
| Input  | $1.50 |
| Output | $8.00 |

## Supported features

`tools`, `json_mode`, `structured_outputs`, `logprobs`

## Sampling parameters

`temperature`, `top_p`, `top_k`, `max_tokens`, `stop`, `seed`, `frequency_penalty`, `presence_penalty`, `repetition_penalty`, `logprobs`, `top_logprobs`

## Example

```bash
curl https://api.tera.gw/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TERA_API_KEY" \
  -d '{
    "model": "openai/gpt-oss-120b",
    "messages": [{"role": "user", "content": "Hello"}],
    "max_tokens": 256
  }'
```
