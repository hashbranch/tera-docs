---
title: "Kimi-K2.5"
description: "moonshotai/Kimi-K2.5 on Tera"
---

<Info>
  Model id: `moonshotai/Kimi-K2.5` — pass this as the `model` field in API requests.
</Info>

## Spec

| | |
|---|---|
| **Provider** | moonshotai |
| **HuggingFace** | [`moonshotai/Kimi-K2.5`](https://huggingface.co/moonshotai/Kimi-K2.5) |
| **Context length** | 262,144 tokens |
| **Max output** | 4,096 tokens |
| **Quantization** | `managed` |
| **Input modalities** | text |
| **Output modalities** | text |

## Pricing

| | per million tokens |
|---|---|
| Input  | $0.60 |
| Output | $3.00 |

## Supported features

`tools`, `json_mode`, `structured_outputs`, `reasoning`

## Sampling parameters

`temperature`, `top_p`, `max_tokens`, `stop`, `seed`, `frequency_penalty`, `presence_penalty`, `logprobs`, `top_logprobs`

## Example

```bash
curl https://api.tera.gw/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TERA_API_KEY" \
  -d '{
    "model": "moonshotai/Kimi-K2.5",
    "messages": [{"role": "user", "content": "Hello"}],
    "max_tokens": 256
  }'
```
